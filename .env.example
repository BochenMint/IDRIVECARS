# Kanoniczny URL (SEO, OG, sitemap)
NEXT_PUBLIC_SITE_URL=https://idrivecars.pl

# Hasło do panelu /admin i API /api/admin/* (Basic Auth, dowolny login)
# Bez tej zmiennej panel jest otwarty — tylko na lokalny dev.
ADMIN_SECRET=
